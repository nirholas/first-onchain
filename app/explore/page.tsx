import type { Metadata } from "next";
import { ExternalLink } from "lucide-react";
import { ExplorerSearch } from "@/components/explorer";
export const metadata:Metadata={title:"Explore"};
const ideas=[{type:"agent",title:"Autonomous agent identity",body:"A signed capabilities manifest with an owner and live endpoint."},{type:"image",title:"A tiny generative artwork",body:"An SVG small enough to live completely inside transaction history."},{type:"json",title:"A public promise",body:"Structured, timestamped words that cannot quietly be edited later."},{type:"text",title:"The first line",body:"A message, dedication, observation, joke, or cultural timestamp."},{type:"coin",title:"A community token",body:"Native onchain metadata and a fixed supply held by its creator."},{type:"address",title:"An identity you recognize",body:"A locally generated Solana address with a memorable prefix."}];
export default function Explore(){return <main><div className="shell"><header className="page-head"><span className="eyebrow"><i/> Chain reader</span><h1>Explore</h1><p>Paste a transaction to decode a First envelope, or borrow an idea and make your own permanent artifact.</p></header><ExplorerSearch/><div className="section-head"><h2>What could<br/>be first?</h2><p>The interesting part is not storing more. It is choosing what deserves permanence.</p></div><div className="explore-grid">{ideas.map((x,i)=><article className="artifact" key={x.title}><div><small>#{String(i+1).padStart(2,"0")} · {x.type}</small><h3>{x.title}</h3><p style={{color:"var(--muted)",lineHeight:1.55}}>{x.body}</p></div><span className="hash">first/1 <ExternalLink size={12}/></span></article>)}</div></div></main>}
